#include"gcs_mavlink.hpp"

uint8_t GCS_MAVLINK::getData(uint8_t len, uint8_t *data)
{
    cout << "GCS_MAVLINK::getData" << endl;
    return 0;
}

uint8_t GCS_MAVLINK::sendData(uint8_t len,const uint8_t* data)
{
    cout << "GCS_MAVLINK::sendData" << endl;
    return 0;
}
